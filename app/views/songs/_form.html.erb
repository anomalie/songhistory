<%= form_with(model: song, local: true) do |form| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :song_title %>
  </div>

  <div class="field">
    <%= form.label :lyrics %>
    <%= form.text_field :lyrics, id: :song_lyrics %>
  </div>

  <div class="field">
    <%= form.label :month %>
    <%= form.text_field :month, id: :song_month %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year, id: :song_year %>
  </div>

  <div class="field">
    <%= form.label :audio %>
    <%= form.text_field :audio, id: :song_audio %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
